# https://developer.hashicorp.com/nomad/docs/configuration/server
server {
  enabled = true
  bootstrap_expect = 1

  server_join {
    retry_join     = {{ nomad_servers | tojson }}
    retry_max      = 3
    retry_interval = "15s"
  }
}